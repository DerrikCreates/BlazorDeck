A StreamDeck / TouchPortal alternative that runs in the browser. 

For now it uses [StreamerBot](https://streamer.bot/) to invoke actions. So if you already use streamerbot or are looking to better control streamerbot then this might help
